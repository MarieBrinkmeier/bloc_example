name: bloc_example

description: The Weather Forecast App is a simple, user-friendly application developed using Flutter. It employs the BLOC (Business Logic Component) pattern for state management, ensuring a clean separation between the UI (User Interface) and business logic.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.1.5 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: 8.1.3
  http: 1.1.0
  rive: 0.12.2
  url_launcher: 6.0.10



dev_dependencies:
  bloc_test: 9.1.5
  flutter_test:
    sdk: flutter
  mocktail: 1.0.1
  very_good_analysis: 5.1.0

flutter:
  uses-material-design: true
  assets:
    - assets/animations/impatient_placeholder.riv